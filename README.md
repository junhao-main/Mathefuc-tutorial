# Mathefuc-tutorial

Math Proof Practice 教程内容，通过 GitHub Pages 发布，供主站拉取渲染。

## 结构

- `manifest.json` — 页面列表与导航结构
- `*.md` — Markdown 教程页面

## 编辑

在 GitHub 网页或本地编辑 `.md` 文件，新增页面时在 `manifest.json` 的 `pages` 中添加条目。
